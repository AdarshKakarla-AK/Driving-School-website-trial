# Runs the automations cron every 30 minutes on the clock.

[Unit]
Description=Run Sri Mathru automation cron every 30 minutes

[Timer]
OnCalendar=*-*-* *:0,30:00
Persistent=true

[Install]
WantedBy=timers.target
